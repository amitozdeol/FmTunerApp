alevt.diff
  Fixes incorrect v4l2 defines (probably derived from older v4l2 API
  revisions). The patch is for version 1.6.1 and this teletext decoder/browser
  can be found here: http://www.goron.de/~froese (although this site seems
  to be down a lot).

MPlayer-1.0pre7try2-ivtvosd.diff
  Adds ivtv and ivtvosd vidouts to MPlayer

cx25840.diff
  Fixes a bug in the cx25840 module in the 2.6.20 kernel. Without this
  patch the cx25840 firmware cannot be loaded.
